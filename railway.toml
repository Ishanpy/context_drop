[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install -r backend/requirements.txt --prefer-binary && uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"
healthcheckPath = "/health"
healthcheckTimeout = 30