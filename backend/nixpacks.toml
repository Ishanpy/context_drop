[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt --prefer-binary --only-binary=:all: psycopg2-binary"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"